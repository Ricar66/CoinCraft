<Window x:Class="CoinCraft.App.Views.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        Title="Dashboard - CoinCraft" MinWidth="1024" MinHeight="768" WindowStartupLocation="CenterOwner"
        Background="{DynamicResource AppBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="KpiLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="KpiValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Menu -->
                <RowDefinition Height="Auto"/> <!-- Header + Filter -->
                <RowDefinition Height="Auto"/> <!-- KPIs -->
                <RowDefinition Height="400"/>  <!-- Main Charts -->
                <RowDefinition Height="Auto"/> <!-- Lists -->
                <RowDefinition Height="Auto"/> <!-- Net Worth -->
            </Grid.RowDefinitions>

            <!-- 0. Menu (Simplified Strip) -->
            <Border Grid.Row="0" Margin="0,0,0,20" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,10">
                <StackPanel Orientation="Horizontal">
                    <Button Content="Início" Click="OnGoDashboard" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,10,0"/>
                    <Button Content="Lançamentos" Click="OnGoTransactions" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,10,0"/>
                    <Button Content="Contas" Click="OnGoAccounts" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,10,0"/>
                    <Button Content="Categorias" Click="OnGoCategories" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,10,0"/>
                    <Button Content="Recorrentes" Click="OnGoRecurring" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,10,0"/>
                    <Button Content="Importar" Click="OnGoImport" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,10,0"/>
                    <Button Content="Ajuda" Click="OnGoManual" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="Bold" Margin="0,0,10,0"/>
                    <Button Content="⚙" Click="OnGoSettings" Background="Transparent" Foreground="{DynamicResource TextPrimaryBrush}" ToolTip="Configurações"/>
                </StackPanel>
            </Border>

            <!-- 1. Header + Actions -->
            <Grid Grid.Row="1" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Title & Filter -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Visão Geral" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,30,0" VerticalAlignment="Center"/>
                    
                    <Border Background="White" CornerRadius="8" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Padding="10,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Período:" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,5,0"/>
                            <DatePicker x:Name="DashFromPicker" BorderThickness="0"/>
                            <TextBlock Text="até" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <DatePicker x:Name="DashToPicker" BorderThickness="0"/>
                            <Button Content="Filtrar" Click="OnApplyDashFilters" Margin="10,0,0,0" Height="30" Padding="15,0"/>
                            <ProgressBar Width="20" Height="20" IsIndeterminate="True" Margin="10,0,0,0" 
                                         Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Actions -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="+ Nova Transação" Click="OnGoTransactions" Margin="0,0,10,0" Height="40" Background="{DynamicResource PrimaryBrush}"/>
                    <Button Content="Exportar Relatório" Command="{Binding ExportReportCommand}" Height="40" Background="Transparent" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Foreground="{DynamicResource TextPrimaryBrush}"/>
                </StackPanel>
            </Grid>

            <!-- 2. KPIs -->
            <Grid Grid.Row="2" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Income -->
                <Border Grid.Column="0" Style="{DynamicResource CardStyle}">
                    <Grid>
                        <StackPanel>
                            <TextBlock Text="Receitas" Style="{DynamicResource KpiLabelStyle}"/>
                            <TextBlock Text="{Binding TotalReceitas, StringFormat=C}" Style="{DynamicResource KpiValueStyle}" Foreground="{DynamicResource SuccessBrush}"/>
                        </StackPanel>
                        <Viewbox Width="32" Height="32" HorizontalAlignment="Right" VerticalAlignment="Top" Opacity="0.2">
                            <Path Fill="{DynamicResource SuccessBrush}" Data="M12,2C17.52,2 22,6.48 22,12C22,17.52 17.52,22 12,22C6.48,22 2,17.52 2,12C2,6.48 6.48,2 12,2M12,20C16.41,20 20,16.41 20,12C20,7.59 16.41,4 12,4C7.59,4 4,7.59 4,12C4,16.41 7.59,20 12,20M11,7H13V13H16.2L12,17.2L7.8,13H11V7Z" />
                        </Viewbox>
                    </Grid>
                </Border>

                <!-- Expense -->
                <Border Grid.Column="1" Style="{DynamicResource CardStyle}">
                    <Grid>
                        <StackPanel>
                            <TextBlock Text="Despesas" Style="{DynamicResource KpiLabelStyle}"/>
                            <TextBlock Text="{Binding TotalDespesas, StringFormat=C}" Style="{DynamicResource KpiValueStyle}" Foreground="{DynamicResource DangerBrush}"/>
                        </StackPanel>
                        <Viewbox Width="32" Height="32" HorizontalAlignment="Right" VerticalAlignment="Top" Opacity="0.2">
                            <Path Fill="{DynamicResource DangerBrush}" Data="M12,2C17.52,2 22,6.48 22,12C22,17.52 17.52,22 12,22C6.48,22 2,17.52 2,12C2,6.48 6.48,2 12,2M12,20C16.41,20 20,16.41 20,12C20,7.59 16.41,4 12,4C7.59,4 4,7.59 4,12C4,16.41 7.59,20 12,20M11,17H13V11H16.2L12,6.8L7.8,11H11V17Z" />
                        </Viewbox>
                    </Grid>
                </Border>

                <!-- Balance -->
                <Border Grid.Column="2" Style="{DynamicResource CardStyle}" Margin="0,0,0,20">
                    <Grid>
                        <StackPanel>
                            <TextBlock Text="Saldo do Período" Style="{DynamicResource KpiLabelStyle}"/>
                            <TextBlock Text="{Binding TotalSaldo, StringFormat=C}" Style="{DynamicResource KpiValueStyle}" Foreground="{DynamicResource PrimaryBrush}"/>
                        </StackPanel>
                        <Viewbox Width="32" Height="32" HorizontalAlignment="Right" VerticalAlignment="Top" Opacity="0.2">
                            <Path Fill="{DynamicResource PrimaryBrush}" Data="M5,6H23V18H5V6M5,4C3.9,4 3,4.9 3,6V18C3,19.1 3.9,20 5,20H23C24.1,20 25,19.1 25,18V6C25,4.9 24.1,4 23,4H5Z" />
                        </Viewbox>
                    </Grid>
                </Border>
            </Grid>

            <!-- 3. Charts Row -->
            <Grid Grid.Row="3" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- Bar Chart -->
                <Border Grid.Column="0" Style="{DynamicResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Comparativo de Fluxo" Style="{DynamicResource HeaderTextStyle}"/>
                        <lvc:CartesianChart Grid.Row="1" Series="{Binding ComparisonSeries}" YAxes="{Binding ComparisonYAxis}" Margin="0,10,0,0"/>
                    </Grid>
                </Border>

                <!-- Pie Chart -->
                <Border Grid.Column="1" Style="{DynamicResource CardStyle}" Margin="0,0,0,20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Despesas por Categoria" Style="{DynamicResource HeaderTextStyle}"/>
                        <lvc:PieChart Grid.Row="1" Series="{Binding PieSeries}" LegendPosition="Bottom" Margin="0,10,0,0"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- 4. Lists Row -->
            <Grid Grid.Row="4" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- Account Balances -->
                <Border Grid.Column="0" Style="{DynamicResource CardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Saldos por Conta" Style="{DynamicResource HeaderTextStyle}"/>
                        <DataGrid Grid.Row="1" ItemsSource="{Binding AccountBalances}" AutoGenerateColumns="False" 
                                  CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="Horizontal" 
                                  HeadersVisibility="Column" Background="White" BorderThickness="0"
                                  RowHeight="35" ColumnHeaderHeight="40">
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                </Style>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="10,0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                                    <ContentPresenter VerticalAlignment="Center" />
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Conta" Binding="{Binding AccountName}" Width="*"/>
                                <DataGridTextColumn Header="Saldo" Binding="{Binding Balance, StringFormat=C}" Width="Auto" FontWeight="Bold"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <!-- Goals -->
                <Border Grid.Column="1" Style="{DynamicResource CardStyle}" Margin="0,0,0,20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Metas do Mês" Style="{DynamicResource HeaderTextStyle}"/>
                        <DataGrid Grid.Row="1" ItemsSource="{Binding GoalsProgress}" AutoGenerateColumns="False" 
                                  CanUserAddRows="False" IsReadOnly="True" GridLinesVisibility="Horizontal" 
                                  HeadersVisibility="Column" Background="White" BorderThickness="0"
                                  RowHeight="35" ColumnHeaderHeight="40">
                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                </Style>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="10,0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                                                    <ContentPresenter VerticalAlignment="Center" />
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Categoria" Binding="{Binding CategoryName}" Width="*"/>
                                <DataGridTextColumn Header="Gasto / Limite" Binding="{Binding Spent, StringFormat=C}" Width="Auto"/>
                                <DataGridTemplateColumn Header="Progresso" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ProgressBar Value="{Binding Percent, Mode=OneWay}" Maximum="1" Height="6" 
                                                         Foreground="{DynamicResource PrimaryBrush}" Background="#F0F0F0" BorderThickness="0"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="%" Binding="{Binding Percent, StringFormat=P0}" Width="50"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>

            <!-- 5. Net Worth Chart -->
            <Border Grid.Row="5" Style="{DynamicResource CardStyle}" Margin="0,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="300"/>
                    </Grid.RowDefinitions>
                    
                    <Grid Grid.Row="0">
                        <TextBlock Text="Evolução do Patrimônio" Style="{DynamicResource HeaderTextStyle}"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="Período:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource TextSecondaryBrush}"/>
                            <ComboBox x:Name="NetWorthPeriodCombo" Width="100" SelectedIndex="1" SelectionChanged="OnNetWorthPeriodChanged" Padding="5">
                                <ComboBoxItem Content="6"/>
                                <ComboBoxItem Content="12"/>
                                <ComboBoxItem Content="24"/>
                            </ComboBox>
                        </StackPanel>
                    </Grid>

                    <lvc:CartesianChart Grid.Row="1" Series="{Binding NetWorthSeries}" XAxes="{Binding NetWorthXAxis}" Margin="0,10,0,0"/>
                </Grid>
            </Border>

        </Grid>
    </ScrollViewer>
</Window>
