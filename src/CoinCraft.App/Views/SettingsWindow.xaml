<Window x:Class="CoinCraft.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurações - CoinCraft" MinWidth="1024" MinHeight="768" WindowStartupLocation="CenterScreen"
        Background="{DynamicResource AppBackgroundBrush}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Menu -->
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Content -->
            <RowDefinition Height="*"/>    <!-- Spacer -->
        </Grid.RowDefinitions>

        <!-- 0. Menu -->
        <Border Grid.Row="0" Margin="0,0,0,20" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <Button Content="Início" Click="OnGoDashboard" Style="{DynamicResource MenuButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="Lançamentos" Click="OnGoTransactions" Style="{DynamicResource MenuButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="Contas" Click="OnGoAccounts" Style="{DynamicResource MenuButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="Categorias" Click="OnGoCategories" Style="{DynamicResource MenuButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="Recorrentes" Click="OnGoRecurring" Style="{DynamicResource MenuButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="Importar" Click="OnGoImport" Style="{DynamicResource MenuButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="Configurações" Click="OnGoSettings" Style="{DynamicResource MenuButtonStyle}" Foreground="{DynamicResource PrimaryBrush}" FontWeight="Bold"/>
            </StackPanel>
        </Border>

        <!-- 1. Header -->
        <TextBlock Grid.Row="1" Text="Configurações do Sistema" Style="{DynamicResource HeaderTextStyle}"/>

        <!-- 2. Content -->
        <Border Grid.Row="2" Style="{DynamicResource CardStyle}" Padding="30" Width="600" HorizontalAlignment="Left" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Preferências Gerais" Style="{DynamicResource SectionHeaderStyle}"/>
                
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Tema:" VerticalAlignment="Center" Margin="0,0,0,16"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" SelectedValue="{Binding Tema}" SelectedValuePath="Content" Margin="0,0,0,16">
                        <ComboBoxItem>claro</ComboBoxItem>
                        <ComboBoxItem>escuro</ComboBoxItem>
                    </ComboBox>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Moeda:" VerticalAlignment="Center" Margin="0,0,0,16"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" SelectedValue="{Binding Moeda}" SelectedValuePath="Content" Margin="0,0,0,16">
                        <ComboBoxItem>BRL</ComboBoxItem>
                        <ComboBoxItem>USD</ComboBoxItem>
                        <ComboBoxItem>EUR</ComboBoxItem>
                    </ComboBox>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Tela Inicial:" VerticalAlignment="Center" Margin="0,0,0,16"/>
                    <ComboBox Grid.Row="2" Grid.Column="1" SelectedValue="{Binding TelaInicial}" SelectedValuePath="Content" Margin="0,0,0,16">
                        <ComboBoxItem>dashboard</ComboBoxItem>
                        <ComboBoxItem>lancamentos</ComboBoxItem>
                    </ComboBox>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="Salvar Alterações" Click="OnSaveClick" Style="{DynamicResource PrimaryButtonStyle}" Width="160"/>
                </StackPanel>
            </StackPanel>
        </Border>
        
        <TextBlock Grid.Row="3" Text="Versão 1.0.1" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,20"/>
    </Grid>
</Window>