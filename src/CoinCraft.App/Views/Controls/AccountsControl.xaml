<UserControl x:Class="CoinCraft.App.Views.Controls.AccountsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal" Grid.Row="0">
      <TextBlock Text="Contas" FontSize="20" FontWeight="Bold"/>
    </StackPanel>
    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,8,0,8">
      <Button Content="Adicionar" Click="OnAddClick"/>
      <Button Content="Editar" Margin="8,0,0,0" Click="OnEditClick"/>
      <Button Content="Excluir" Margin="8,0,0,0" Click="OnDeleteClick"/>
      <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="12,0,0,0"/>
    </StackPanel>
    <DataGrid Grid.Row="2" ItemsSource="{Binding Accounts}" AutoGenerateColumns="False" IsReadOnly="True" SelectedItem="{Binding SelectedAccount}">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
        <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="120"/>
        <DataGridTextColumn Header="Saldo Inicial" Binding="{Binding SaldoInicial}" Width="140"/>
        <DataGridCheckBoxColumn Header="Ativa" Binding="{Binding Ativa}" Width="80"/>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>