<Window x:Class="CoinCraft.Admin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CoinCraft Admin" Height="560" Width="700" WindowStartupLocation="CenterScreen"
        Background="#121216" Foreground="#EAEAF2">
    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#7C4DFF"/>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#303030"/>
            <Setter Property="Foreground" Value="#EAEAF2"/>
            <Setter Property="BorderBrush" Value="#4A4A59"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#D6D6E0"/>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="2" Text="Gerador de Licenças CoinCraft" FontSize="20" FontWeight="Bold"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Remetente (Seu e-mail):" VerticalAlignment="Center"/>
        <TextBox x:Name="SenderEmailText" Grid.Row="1" Grid.Column="1" Margin="8,4,0,4" ToolTip="Seu e-mail de envio (ex.: Gmail)"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Senha de App:" VerticalAlignment="Center"/>
        <PasswordBox x:Name="SenderAppPasswordBox" Grid.Row="2" Grid.Column="1" Margin="8,4,0,4" ToolTip="Senha de app (Gmail/Outlook)"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Nome do Cliente:" VerticalAlignment="Center"/>
        <TextBox x:Name="NameText" Grid.Row="3" Grid.Column="1" Margin="8,4,0,4"/>

        <Label Grid.Row="4" Grid.Column="0" Content="E-mail:" VerticalAlignment="Center"/>
        <TextBox x:Name="EmailText" Grid.Row="4" Grid.Column="1" Margin="8,4,0,4"/>

        <Label Grid.Row="5" Grid.Column="0" Content="Hardware ID:" VerticalAlignment="Center"/>
        <TextBox x:Name="HardwareText" Grid.Row="5" Grid.Column="1" Margin="8,4,0,4" ToolTip="Cole o Hardware ID enviado pelo cliente"/>

        <Button x:Name="GenerateSendButton" Grid.Row="6" Grid.ColumnSpan="2" Content="Gerar e Enviar por E-mail"
                Height="44" Margin="0,12,0,12" Click="OnGenerateAndSendClick"/>

        <TextBlock Grid.Row="7" Grid.ColumnSpan="2" Text="Licença gerada:" Margin="0,8,0,4"/>
        <TextBox x:Name="LicenseOutput" Grid.Row="8" Grid.ColumnSpan="2" Margin="0,0,0,0" Height="160" IsReadOnly="True"
                 Background="#1A1A22" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" TextWrapping="Wrap"/>

        <StatusBar Grid.Row="9" Grid.ColumnSpan="2" Background="#1A1A22" Foreground="#D6D6E0">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Pronto"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
