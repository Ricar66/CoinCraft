<Window x:Class="CoinCraft.App.Views.TransactionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lançamentos" MinWidth="900" MinHeight="600" WindowStartupLocation="CenterOwner">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Content="Adicionar" Click="OnAddClick" Margin="0,0,8,0"/>
            <Button Content="Editar" Click="OnEditClick" Margin="0,0,8,0"/>
            <Button Content="Excluir" Click="OnDeleteClick" Margin="0,0,8,0"/>
            <Separator/>
            <Button Content="Atualizar" Click="OnRefreshClick"/>
            <Separator/>
            <Button Content="Exportar CSV" Click="OnExportCsvClick"/>
            <Button Content="Exportar PDF" Click="OnExportPdfClick"/>
        </ToolBar>
        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock Text="{Binding StatusMessage}"/>
        </StatusBar>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="8">
            <StackPanel.Resources>
                <Style TargetType="FrameworkElement">
                    <Setter Property="Margin" Value="0,0,8,0"/>
                </Style>
            </StackPanel.Resources>
            <TextBlock Text="De:" VerticalAlignment="Center"/>
            <DatePicker x:Name="FromPicker" Width="140"/>
            <TextBlock Text="Até:" VerticalAlignment="Center"/>
            <DatePicker x:Name="ToPicker" Width="140"/>
            <TextBlock Text="Conta:" VerticalAlignment="Center"/>
            <ComboBox x:Name="AccountFilter" Width="200" DisplayMemberPath="Nome" SelectedValuePath="Id"/>
            <TextBlock Text="Categoria:" VerticalAlignment="Center"/>
            <ComboBox x:Name="CategoryFilter" Width="200" DisplayMemberPath="Nome" SelectedValuePath="Id"/>
            <Button Content="Aplicar" Click="OnApplyFilters"/>
        </StackPanel>

        <Grid Margin="8">
            <DataGrid ItemsSource="{Binding Transactions}"
                      SelectedItem="{Binding Selected, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      SelectionMode="Single">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Data" Binding="{Binding Data, StringFormat=d}" Width="120"/>
                    <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="120"/>
                    <DataGridTextColumn Header="Valor" Binding="{Binding Valor}" Width="120"/>
                    <DataGridTextColumn Header="Conta" Binding="{Binding AccountName}" Width="*"/>
                    <DataGridTextColumn Header="Categoria" Binding="{Binding CategoryName}" Width="200"/>
                    <DataGridTextColumn Header="Descrição" Binding="{Binding Descricao}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>
