<Window x:Class="CoinCraft.App.Views.CategoriesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Categorias" MinWidth="900" MinHeight="600" WindowStartupLocation="CenterOwner">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button Content="Adicionar" Click="OnAddClick" Margin="0,0,8,0"/>
            <Button Content="Editar" Click="OnEditClick" Margin="0,0,8,0"/>
            <Button Content="Excluir" Click="OnDeleteClick" Margin="0,0,8,0"/>
            <Separator/>
            <Button Content="Atualizar" Click="OnRefreshClick"/>
        </ToolBar>
        <StatusBar DockPanel.Dock="Bottom">
            <TextBlock Text="{Binding StatusMessage}"/>
        </StatusBar>
        <Grid Margin="8">
            <DataGrid ItemsSource="{Binding Categories}"
                      SelectedItem="{Binding Selected, Mode=TwoWay}"
                      AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                    <DataGridTextColumn Header="Cor" Binding="{Binding CorHex}" Width="160"/>
                    <DataGridTextColumn Header="Ícone" Binding="{Binding Icone}" Width="160"/>
                    <DataGridTextColumn Header="Subcategoria de" Binding="{Binding ParentCategoryId}" Width="160"/>
                    <DataGridTextColumn Header="Limite Mensal" Binding="{Binding LimiteMensal}" Width="160"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </DockPanel>
</Window>
