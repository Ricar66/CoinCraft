<UserControl x:Class="CoinCraft.App.Views.Controls.CategoriesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <StackPanel Orientation="Horizontal" Grid.Row="0">
      <TextBlock Text="Categorias" FontSize="20" FontWeight="Bold"/>
    </StackPanel>
    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,8,0,8">
      <Button Content="Adicionar" Click="OnAddClick"/>
      <Button Content="Editar" Margin="8,0,0,0" Click="OnEditClick"/>
      <Button Content="Excluir" Margin="8,0,0,0" Click="OnDeleteClick"/>
      <TextBlock x:Name="StatusText" VerticalAlignment="Center" Margin="12,0,0,0"/>
    </StackPanel>
    <DataGrid Grid.Row="2" ItemsSource="{Binding Categories}" AutoGenerateColumns="False" IsReadOnly="True" SelectedItem="{Binding Selected}">
      <DataGrid.Columns>
        <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
        <DataGridTextColumn Header="Cor" Binding="{Binding CorHex}" Width="120"/>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>