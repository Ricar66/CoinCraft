<Window x:Class="CoinCraft.App.Views.TransactionEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Lançamento" Height="420" Width="600" WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Data" VerticalAlignment="Center"/>
            <DatePicker x:Name="DataPicker" Grid.Column="1" Margin="8,0,0,8" HorizontalAlignment="Stretch"/>

            <TextBlock Text="Tipo" Grid.Row="1" VerticalAlignment="Center"/>
            <ComboBox x:Name="TipoCombo" Grid.Row="1" Grid.Column="1" Margin="8,0,0,8" HorizontalAlignment="Stretch"/>

            <TextBlock Text="Valor" Grid.Row="2" VerticalAlignment="Center"/>
            <TextBox x:Name="ValorBox" Grid.Row="2" Grid.Column="1" Margin="8,0,0,8" HorizontalAlignment="Stretch"/>

            <TextBlock Text="Conta" Grid.Row="3" VerticalAlignment="Center"/>
            <ComboBox x:Name="ContaCombo" Grid.Row="3" Grid.Column="1" Margin="8,0,0,8" DisplayMemberPath="Nome" SelectedValuePath="Id" HorizontalAlignment="Stretch"/>

            <TextBlock Text="Categoria" Grid.Row="4" VerticalAlignment="Center"/>
            <ComboBox x:Name="CategoriaCombo" Grid.Row="4" Grid.Column="1" Margin="8,0,0,8" DisplayMemberPath="Nome" SelectedValuePath="Id" HorizontalAlignment="Stretch"/>

            <TextBlock Text="Descrição" Grid.Row="5" VerticalAlignment="Center"/>
            <TextBox x:Name="DescricaoBox" Grid.Row="5" Grid.Column="1" Margin="8,0,0,8" HorizontalAlignment="Stretch"/>

            <TextBlock Text="Conta Destino (Transferência)" Grid.Row="6" VerticalAlignment="Center"/>
            <ComboBox x:Name="ContaDestinoCombo" Grid.Row="6" Grid.Column="1" Margin="8,0,0,8" DisplayMemberPath="Nome" SelectedValuePath="Id" HorizontalAlignment="Stretch"/>

            <TextBlock Text="Comprovante" Grid.Row="7" VerticalAlignment="Center"/>
            <StackPanel Grid.Row="7" Grid.Column="1" Orientation="Horizontal" Margin="8,0,0,8">
                <Button x:Name="SelectAttachmentButton" Content="Selecionar arquivo…" Click="OnSelectAttachmentClick"/>
                <TextBlock x:Name="AttachmentNameText" Margin="8,0,0,0" VerticalAlignment="Center" Text="Nenhum arquivo selecionado"/>
            </StackPanel>

            <StackPanel Grid.Row="8" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Content="Cancelar" IsCancel="True" Margin="0,0,8,0"/>
                <Button Content="Salvar" Click="OnSaveClick" IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
