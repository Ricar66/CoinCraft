<Window x:Class="CoinCraft.App.Views.ActivationMethodWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CoinCraft.App.ViewModels"
        mc:Ignorable="d"
        Title="Bem-vindo ao CoinCraft" Height="600" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" WindowStyle="SingleBorderWindow"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Logo/Header -->
            <RowDefinition Height="*"/>    <!-- Content -->
            <RowDefinition Height="Auto"/> <!-- Footer -->
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,32">
            <TextBlock Text="CoinCraft" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center" Foreground="{DynamicResource PrimaryBrush}"/>
            <TextBlock Text="Bem-vindo ao CoinCraft" FontSize="16" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Como deseja ativar seu produto?" FontSize="15" FontWeight="Medium" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                
                <!-- Option A: Email Panel -->
                <Border Background="{DynamicResource CardBackgroundBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="E-mail de compra" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>
                        
                        <Button Content="Ativar Online" Command="{Binding ValidateEmailCommand}" Style="{DynamicResource PrimaryButtonStyle}">
                            <!-- Removed internal style triggers to avoid StaticResource dependency -->
                        </Button>
                        
                        <ProgressBar IsIndeterminate="True" Height="4" Margin="0,8,0,0" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource DangerBrush}" TextWrapping="Wrap" HorizontalAlignment="Center" FontSize="12" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
                
                <!-- Option B: Manual Key -->
                <Button Content="Já tenho uma chave (Digitar Manualmente)" Command="{Binding SwitchToOfflineModeCommand}" Style="{DynamicResource SecondaryButtonStyle}"/>
            </StackPanel>
        </ScrollViewer>
        
        <StackPanel Grid.Row="2" Margin="0,0,0,0">
            <Separator Background="{DynamicResource BorderBrush}" Margin="0,0,0,12"/>
            <TextBlock Text="Precisa de ajuda? suporte@coincraft.com" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
